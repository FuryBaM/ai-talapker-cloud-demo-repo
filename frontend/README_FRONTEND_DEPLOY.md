# Frontend service

Static chat/admin UI served by nginx.

Render settings:

```text
Runtime: Docker
Root Directory: frontend
```

Required env:

```env
BACKEND_API_BASE=https://YOUR-BACKEND.onrender.com
```
