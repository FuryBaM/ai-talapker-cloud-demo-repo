# Optional dependencies for assistant/train_sft.py (LoRA/QLoRA SFT)
# Install only in the training environment, not necessarily on the FastAPI server.
peft>=0.13.0
bitsandbytes>=0.43.3; platform_system != "Windows"
trl>=0.11.4
# datasets is not required by train_sft.py, but useful for notebook experiments.
datasets>=2.21.0
